const {resolve} = require('path');

module.exports = {
    config: resolve(__dirname, 'src', 'config', 'database.js'),
    'models-pah': resolve(__dirname, 'src', 'app', 'models'),
    'migrations-pah': resolve(__dirname, 'src', 'database', 'migrations'),
    'seeders-pah': resolve(__dirname, 'src', 'database', 'seeds'),
}
